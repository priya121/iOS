import Quick
import Nimble

class FooSpec: QuickSpec {
    override func spec() {

        describe("A sample test") {
            it("passes") {
                expect(1).to(equal(1))
            }
        }

    }
}
