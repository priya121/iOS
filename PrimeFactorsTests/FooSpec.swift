import Quick
import Nimble
import PrimeFactors

class FooSpec: QuickSpec {
    override func spec() {

        describe("Foo") {
            it("has a sample test") {
                expect(1).to(equal(2))
            }
        }

    }
}
